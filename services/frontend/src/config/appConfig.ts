export const APP_ID = (import.meta.env.VITE_APP_ID || 'nova-app') as string;

export const DEFAULT_QUIZ_QUESTION_COUNT = 5;

export const DEFAULT_NOTE_SESSION_ID = (import.meta.env.VITE_NOTE_SESSION_ID || 'self-study') as string;

export const DEFAULT_NOTE_RETENTION_DAYS = 365;
